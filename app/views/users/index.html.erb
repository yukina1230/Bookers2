<h2>Users</h2>
<table>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
    </tr>
  </thead>
  <tbody>
      <%= @users.each do |user| %>
      <tr>
          <th>投稿ユーザー画像</th>
          <th><%= user.name %></th>
          <th><%= link_to "Show",user_path(user.id) %></th>
      </tr>
      <% end %>
  </tbody>
</table>
